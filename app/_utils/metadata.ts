export const pageTitle = "Poszczekane"

export const contactEmail = 'kontakt@poszczekane.pl'

export const supportEmail = 'kontakt@poszczekane.pl'

export function metaTitle(title: string): string {
    return `${title} | ${pageTitle}`
}